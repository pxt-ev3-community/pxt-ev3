# Read value

Reads a stored value from this project's save file.

## Example

```blocks
storage.readValue("name")
```

## Parameters

### name

The name that identifies the value to be stored.
Multiple values with different names can be stored for each project.

## Returns

The number read, or default value if value has not been stored before.


